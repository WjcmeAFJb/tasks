package org.tasks.jobs

import org.junit.Assert.assertEquals
import org.junit.Test

class UpdateCalendarWorkTest {

    @Test
    fun extraIdConstant() {
        assertEquals("extra_id", UpdateCalendarWork.EXTRA_ID)
    }
}
